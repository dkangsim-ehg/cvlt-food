1. Use the `test_input.csv` file in the `input` folder as a template to record verbal recall tasks.  
2. Use the `cvlt_scoring_ver1.R` file to run the functions. The `source()` function will call custom functions for scoring. 
3. All functions have been tested and confirmed to be working. 
